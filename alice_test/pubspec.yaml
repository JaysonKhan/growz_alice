name: alice_test
description: "Test utils for Alice packages."
version: 1.0.1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  alice:
    path: ../alice
  test:
  mocktail:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
